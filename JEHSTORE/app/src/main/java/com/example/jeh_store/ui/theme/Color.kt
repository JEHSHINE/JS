package com.example.jeh_store.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary Brand Colors (Orange) ─────────────────────────────
val Orange500 = Color(0xFFF97316)      // Main Brand Orange
val Orange700 = Color(0xFFEA580C)      // Dark Orange Hover
val Orange100 = Color(0x1AF97316)      // Soft Orange (rgba(249,115,22,0.1))
val Orange200 = Color(0x33F97316)      // Orange Glow (rgba(249,115,22,0.2))

// ── Dark Theme / Sidebar Colors ───────────────────────────────
val DarkNavy = Color(0xFF0F172A)       // Sidebar Background
val NearBlack = Color(0xFF111827)      // Dark Text
val DarkSlate = Color(0xFF0F172A)      // Main Text
val GrayBlue = Color(0xFF64748B)       // Muted Text
val SoftGray = Color(0xFF6B7280)       // Secondary Text

// ── Background Colors ─────────────────────────────────────────
val LightGray = Color(0xFFF5F7FB)      // Main Background
val White = Color(0xFFFFFFFF)           // Card Background
val GrayWhite = Color(0xFFF8FAFC)      // Soft Background
val WarmWhite = Color(0xFFFFF7ED)      // Hover Background

// ── Success Colors (Green) ────────────────────────────────────
val Green600 = Color(0xFF16A34A)       // Success Main
val Green700 = Color(0xFF059669)       // Success Dark
val Green100 = Color(0xFFDCFCE7)       // Success Background
val Green200 = Color(0xFFD1FAE5)       // Completed Status

// ── Error / Danger Colors (Red) ───────────────────────────────
val Red600 = Color(0xFFDC2626)         // Danger Main
val Red700 = Color(0xFFB91C1C)         // Danger Dark
val Red100 = Color(0xFFFEE2E2)         // Danger Background
val Red800 = Color(0xFF991B1B)         // Error Border

// ── Warning Colors (Amber) ────────────────────────────────────
val Amber500 = Color(0xFFF59E0B)       // Warning Main
val Amber600 = Color(0xFFD97706)       // Warning Dark
val Amber100 = Color(0xFFFEF3C7)       // Warning Background

// ── Supporting Accent Colors (Blue/Cyan) ──────────────────────
val Blue500 = Color(0xFF3B82F6)        // Blue Accent
val Blue900 = Color(0xFF1E3A8A)        // Blue Dark
val Cyan500 = Color(0xFF06B6D4)        // Cyan Accent
val Cyan50  = Color(0xFFECFEFF)        // Info Background

// ── Legacy (removed) colors removed ───────────────────────────